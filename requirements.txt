# Núcleo de deep learning.
# IMPORTANTE: instala torch/torchvision con soporte CUDA siguiendo el README
# (el comando depende de tu versión de CUDA). Estas líneas funcionan para CPU
# y también las sobreescribe la instalación CUDA del README.
torch>=2.1
torchvision>=0.16

numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
opencv-python>=4.8
Pillow>=10.0
tqdm>=4.66

# Descarga del dataset (opcional)
kaggle>=1.6
